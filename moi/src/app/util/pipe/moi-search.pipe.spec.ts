import { MoiSearchPipe } from './moi-search.pipe';

describe('MoiSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MoiSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
