import {Base} from '../base';
export class Person extends Base {

  personId: number;
  about: string;
  address1: string;
  address2: string;
  district: string;
  firstName: string;
  lastName: string;
  village: string;
  zipCode: number;
  city: string;
  country: string;
  state: string;
  // reviews;
  // timelines;
}
